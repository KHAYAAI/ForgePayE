{
  "name": "@forge/langgraph",
  "version": "0.1.0",
  "description": "FORGE Agent Credit Bureau gate for LangGraph — block tool execution when an agent's credit grade is below threshold. Metered at $2.80 per score inquiry.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.3"
  }
}
