apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-whitelabel.fullname" . }}
  labels:
    {{- include "bank-whitelabel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-whitelabel.selectorLabels" . | nindent 4 }}
