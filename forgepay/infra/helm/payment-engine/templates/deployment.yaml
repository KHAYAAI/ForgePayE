apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "payment-engine.fullname" . }}
  labels:
    {{- include "payment-engine.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "payment-engine.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "payment-engine.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: payment-engine
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.env.PORT | quote }}
            - name: ENVIRONMENT
              value: {{ .Values.env.ENVIRONMENT | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.env.LOG_LEVEL | quote }}
            - name: POSTGRES_HOST
              value: {{ .Values.global.postgresql.host | default "forgepay-postgresql" | quote }}
            - name: POSTGRES_PORT
              value: {{ .Values.global.postgresql.port | default 5432 | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.global.postgresql.database | default "forgepay" | quote }}
            - name: POSTGRES_USER
              value: "forgepay"
            - name: VAULT_URL
              value: {{ .Values.hyperswitch.vaultUrl | quote }}
          envFrom:
            - secretRef:
                name: {{ .Values.secretRef }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
