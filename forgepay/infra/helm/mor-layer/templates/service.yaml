apiVersion: v1
kind: Service
metadata:
  name: {{ include "mor-layer.fullname" . }}
  labels:
    {{- include "mor-layer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mor-layer.selectorLabels" . | nindent 4 }}
