apiVersion: v1
kind: Service
metadata:
  name: institutional-reporting
  labels:
    app: institutional-reporting
spec:
  type: {{ .Values.service.type }}
  selector:
    app: institutional-reporting
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
