apiVersion: v1
kind: Service
metadata:
  name: {{ include "unified-router.fullname" . }}
  labels:
    {{- include "unified-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unified-router.selectorLabels" . | nindent 4 }}
