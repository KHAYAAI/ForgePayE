apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-enterprise-treasury
  labels:
    app: enterprise-treasury
    app.kubernetes.io/name: enterprise-treasury
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: enterprise-treasury
    app.kubernetes.io/instance: {{ .Release.Name }}
