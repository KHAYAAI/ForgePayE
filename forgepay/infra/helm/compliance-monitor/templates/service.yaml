apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-monitor.fullname" . }}
  labels:
    {{- include "compliance-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compliance-monitor.selectorLabels" . | nindent 4 }}
