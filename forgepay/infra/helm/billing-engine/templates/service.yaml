apiVersion: v1
kind: Service
metadata:
  name: {{ include "billing-engine.fullname" . }}
  labels:
    {{- include "billing-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "billing-engine.selectorLabels" . | nindent 4 }}
